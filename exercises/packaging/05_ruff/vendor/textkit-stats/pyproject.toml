[project]
name = "textkit-stats"
version = "0.1.0"
description = "Internal stats helper for textkit — not published anywhere"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["textkit_stats"]
